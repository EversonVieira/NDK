@typeparam T

@for(int i = 0; i < DataSource.Count() - 1; i++)
{
    var item = DataSource.ElementAt(i);

    if (ByItem is not null)
    {
        ByItem(item);
    }

    if (ByItemAndIndex is not null)
    {
        ByItemAndIndex((item,i));
    }
}



